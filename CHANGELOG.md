# 更新日志（CHANGELOG）

## 版本号规则（语义化版本 x.y.z）

- **MAJOR（x）**：不兼容的重大重构、数据结构/接口破坏性变更
- **MINOR（y）**：新增功能（新能力、新入口、新界面元素）
- **PATCH（z）**：缺陷修复、文案/样式调整、性能优化

版本号统一定义在 `config.py` 的 `VERSION`，三端展示（CLI `--version`、Web 页脚/统计弹窗、离线 H5 页脚）。
每次按规则改动逻辑后，修改 `VERSION` 并在本文件顶部追加记录（新版本在上）。

---

## 1.4.2（2026-08-20）

### Web 服务保活（修复 + 新能力）
- 修复 webui.py 缺少 `import sys` 导致 pythonw 独立启动崩溃
- 新增 `py gcal.py ensure-web` 看门狗：检测 8333 端口，未监听则以**独立进程**（pythonw，DETACHED）拉起 Web 服务，不再依赖会话/后台任务存活
- 计划任务新增 `GcoresCalendarWatchdog`（每小时自动检测并重启 Web 服务，实测 Result=0）
- `install_task.ps1` 增加登录自启 Web 任务（AtLogOn，需管理员权限创建，普通权限下跳过由看门狗兜底）

---

## 1.4.1（2026-08-16）

### 修复
- 热榜浮窗（#hotModal）缺少 CSS 隐藏规则导致常驻页面 —— 已补 `display:none` 与遮罩样式，**仅在点击"🔥 热榜"按钮后弹出显示**（含浅色主题遮罩适配）

---

## 1.4.0（2026-08-16）

### P0 数据守护（工程完善）
- 每日"一条龙"命令 `py gcal.py daily`：① 数据库备份（`data/backups/`，保留 7 份）→ ② 增量抓取 → ③ 播放量快照 → ④ 完整性自检（逐分类比对 API 计数并告警）
- 计划任务每日动作升级为 `daily`；日志轮转（RotatingFileHandler 5MB×5）
- SQLite 连接统一 `busy_timeout=10s`，并发任务不再互相锁死
- 修复：重跑深扫会清空期数节目归属（album_id）的问题（upsert 改为 COALESCE 保留）

### P1 检索与浏览（新功能）
- **拼音/同音容错检索**：零依赖拼音表（assets/pinyin.txt），`ximeng` → 西蒙；"宫崎竣" → 宫崎骏（在线 + 离线 H5）
- **搜索命中高亮 + 上下文片段预览**
- **组合过滤**：分类 / 频道 / 仅付费 / 日期范围（Web 过滤栏 + CLI `--category/--album/--paid/--from/--to`）
- **频道浏览**：Web"📻 频道"页（252 频道网格 + 频道内期数分页）；CLI `py gcal.py channel <id>`
- **播放量趋势与热榜**：`plays_history` 每日快照（幂等），Web"🔥 热榜"弹窗 + CLI `py gcal.py hot`

---

## 1.3.0（2026-08-16）

### 终端显示：机核有声书另列
- `today` / `day` 命令默认在正文下方**另列**当日有声书（紧凑单行：日期 | 标题 | 链接），主列表不再混入
- `--audiobooks` 参数保留，切换为完整卡片详情显示
- `month` 月历命令补充每日"📖有声书 N 期另列"提示

---

## 1.2.0（2026-08-16）

### 快速跳转日历
- 统计条中的"索引范围"改造为**快速跳转日历**：支持手动输入 `yyyy-mm-dd`（回车/跳转按钮）与**系统自带日历**（原生 date 选择器）两种方式
- 跳转后：月历切换到目标月份 + "历史上的今天"展示目标日期全部历史期数（在线与离线 H5 一致）
- 输入校验：非法日期红框提示（含 2 月 29 日等边界判断）

---

## 1.1.0（2026-08-16）

### UI 按机核官方设计规范重制
- 抓取并分析机核官方样式表（static.gcores.com/assets/Main.css），提取设计 token：
  品牌红 `#ff3d1d` / 品牌 `#f83055`、GPASS 紫 `#5e4bff`、高亮蓝 `#478cfe`、付费金 `#dbc1a1`
  主文本 `#323232` / 次级 `#5a5a5a` / 弱文本 `#aaa`、浅色背景 `#f7f7f8`、暗色面 `#141414 / #1e1e20 / #282828`
- 白天/夜间双主题色板全部替换为机核官方配色
- 字体栈改用官方字体：`PingFangSC, Open Sans, Helvetica Neue, Arial, Hiragino Sans GB, Microsoft YaHei, WenQuanYi Micro Hei, sans-serif`
- 引入官方图标：favicon（16/32px）与页头 logo（assets/ 目录），Web 版经 /assets 静态路由、离线 H5 内联 base64
- 付费徽标采用官方 btn-pay 风格（金字 `#dbc1a1` + 深灰底 `#323232`）；"历史上的今天"按钮、分类条形图使用品牌红

---

## 1.0.0（当前版本）

首个完整基线版本，包含以下功能：

### 数据抓取
- 全量回填：播客频道（252，官方 211）、分类（27）、全量期数（官方已发布 6419，含付费 1263）
- 分类接口补抓，付费/会员期数完整收录（录音笔系列 VOL.1~764 全部在库）
- 期数元数据：标题/副标题/简介/分类/所属频道/时长/播放数/评论数/封面头图
- 参与者名单 + 头像 + 个人主页链接；每期高权重（score）精华评论前三
- 断点续抓（深扫/归属/评论）；每日增量（新期数 + 近 30 天评论/播放刷新）

### Web 界面（py gcal.py serve，默认 127.0.0.1:8333）
- 月历置顶（格点预览链接、期数计数、有声书计数提示）
- 历史上的今天（默认当天、全部期数倒序展开、无年份标签）
- 期数卡片：封面右显、付费徽标、时长/评论/播放（付费显示"--（会员专享）"）、参与者头像、精华评论
- 机核有声书隔离（主列表排除，默认隐藏下拉框单独列出）
- 关键词搜索（FTS5 trigram + 短词回退）+ 按参与者昵称搜索 + 实时提示（词表/标题/参与者）
- 一键"⚡ 立即抓取最新"（后台增量抓取 + 轮询反馈 + 自动刷新视图）
- 统计弹窗（核心指标 + 分类分布条形图 + 抓取时间线）
- 白天/夜间双主题（记忆选择、跟随系统）
- 图片防盗链处理（no-referrer）

### 离线 H5 单文件（py gcal.py export-h5）
- 双击打开的自包含 HTML（内嵌索引快照：期数/评论/参与者/关键词），与在线版同款界面

### 命令行
- init / backfill / categories / sweep / membership / comments / incremental / keywords
- today / day / month / search / suggest / stats / serve / export-h5
- 选项：--all（含非官方）、--audiobooks（含有声书）、--year/--page、--port

### 定时任务
- Windows 计划任务：每日 12:00 增量校验 + 每 6 小时轻量增量（install_task.ps1 一键安装）

### 合规
- 仅公开元数据与公开评论文本，不下载/不存储音频；遵循 robots.txt；低频限速（1 请求/秒 + 抖动）
